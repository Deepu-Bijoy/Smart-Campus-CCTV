# ──────────────────────────────────────────────
# AI-Powered Smart CCTV Investigation System
# Backend Dependencies
#
# IMPORTANT: Install using setup_env.ps1
# PyTorch and lapx must be installed BEFORE this file
# via the setup script — they require special index URLs.
# ──────────────────────────────────────────────

# ── Web Framework ──────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
python-multipart>=0.0.9
gunicorn>=21.2.0

# ── Data Validation ────────────────────────────
pydantic[email]>=2.6.0
pydantic-settings>=2.2.0

# ── Database ───────────────────────────────────
sqlalchemy[asyncio]>=2.0.28
aiosqlite>=0.20.0
asyncpg>=0.29.0
alembic>=1.13.1

# ── Auth & Security ────────────────────────────
pyjwt>=2.8.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.1.0

# ── Task Queue ─────────────────────────────────
celery>=5.3.6
redis>=5.0.3

# ── Computer Vision ────────────────────────────
# NOTE: torch and torchvision are installed separately
# via setup_env.ps1 from the PyTorch CUDA index.
opencv-python-headless>=4.9.0
pillow>=10.2.0
ultralytics>=8.2.0

# ── ByteTrack / Tracking ───────────────────────
# lapx is a drop-in replacement for lap with prebuilt
# Windows wheels for Python 3.13. It is 100% API-compatible.
lapx>=0.5.10
filterpy>=1.4.5
scipy>=1.13.0
supervision>=0.21.0

# ── Re-Identification ──────────────────────────
# torchreid is installed from GitHub source by setup_env.ps1

# ── Face Recognition ───────────────────────────
insightface>=0.7.3
onnxruntime-gpu>=1.18.0

# ── Vector Store ───────────────────────────────
qdrant-client>=1.8.0

# ── NLP / Embeddings ───────────────────────────
transformers>=4.38.0

# ── Utilities ─────────────────────────────────
numpy>=1.26.0
requests>=2.31.0
python-dotenv>=1.0.0
httpx>=0.27.0
aiofiles>=23.2.1
tensorboard>=2.17.0
